# 内容优化执行结果

2026-09-06。已将优化策略推广到全部 88 篇，完成全文论证审阅、重复章节归并、例子修订、架构方案与图谱更新。

## 内容与结构

- 本次逐篇阅读 88 篇全文，修订 76 篇正文、1 篇仅校准元数据，其余 11 篇复核后保留。[完整索引与归并记录](content-full-review.md)说明文章分工；[八项编辑卡](content-full-review.json)记录问题、读者、前提、结论、论据、反例、工程影响及边界。
- 保持知识域 → 专题 → 文章三级结构：4 个知识域、17 个专题、88 篇文章。逐专题区分内容简介与读后产物；“Agent 竞品分析与选型”聚焦比较与采用。
- 重复内容按章节归并：并发机制从恢复篇移入并发主文；统计、成本、项目机制各自保留完整解释；竞品方法删除产品重复介绍；办公采用改为月报交付、写回和异常接管。不为缩小数量删除仍有独立问题的整篇文章，原有 81 条重定向继续有效。
- 修正审批时序、异步归档、插件卸载、定时恢复和记忆有效时间等实质逻辑问题；删除无评分依据的雷达图、悬空 KANO／线框引用，以及项目文章反复出现的原则总结。
- 本次净减少 12,357 个正文字符，同时补充场景、接口和必要前提。字符统计含 Markdown、链接与代码；不将删字比例视为质量分数。
- 全站现有 106 幅 Mermaid 图。新增共同架构正常时序、办公交付流程和评测门槛流程，修正已有图的责任与状态表达；图、正文和代码分别注明实际实现与设计范围。

本次统计相对全量审阅开始时的 88 篇快照。上一轮重点修订、新增架构文章与阅读器修复已包含在该基线中，旧的结构审查记录保留为历史，不替代本次全文审阅。

## 架构方案

[Agent Harness 架构选型](../src/content/writing/harness-architecture-selection.md)给出 A 显式循环、B LangGraph、C Temporal 三种参考组合，包含任务约束、八职责接口、部署与状态归属、正常与故障流程、模块候选、优缺点、服务化责任和迁移条件。

[对照实验](harness-architectures/README.md)真实使用三个控制引擎与 SQLite 业务资源，复用同一资料核验任务、操作身份和验收器。源码、固定依赖、原始结果、[ADR](harness-architectures/ADR.md)与[下载包](../public/labs/harness-architectures.zip)同步。

默认从满足任务约束的显式循环起步；分支与暂停维护成为主要成本时比较 LangGraph；长期等待和执行者接管需要独立管理时比较 Temporal。业务幂等、当前授权和结果验收贯穿三种组合。

## 验证

- 完整构建及内容检查通过：88 篇唯一归属、81 条旧路由、内部引用、阅读时间、代码围栏和图号检查无错误。
- 知识图谱更新为 148 个节点、642 条关系，覆盖全部文章；无重复节点、悬空边或旧文章节点。图谱浏览器交互检查通过。
- 全站浏览器 150 项检查通过，覆盖文章图表渲染、阅读顺序、目录与搜索、移动布局、主题、无 JavaScript 分页和图表缩放；关键新图另有截图复核。
- 三个引擎 × 八个共同场景 × 三次重复，共 72 次契约检查通过；另有 8 项资源与跨进程恢复测试，以及 Temporal 更换 Worker 后恢复检查通过。
- 目录与搜索 16 项单元测试、评测与记忆样例 25 项测试通过；正文 Loop／汇合和 LangGraph 审批示例实际运行通过。
- 架构下载包的 9 个文件与源码逐字节一致；`git diff --check` 通过。

## 验证范围

全文逻辑审阅不等于重新实测全部外部产品。项目事实保留固定版本或文档日期，重点修正对应一手资料；机制分析与采用建议明确标注为推断。

架构实验使用确定性模型替身，不支持真实模型质量、Token 成本或框架性能排名。Temporal 本地服务为内存后端，验证的是服务保持运行时更换 Worker 后恢复；生产容灾、多租户、跨机器和规模吞吐未测。服务部署表是方案，未将设计补写成已验证能力。
