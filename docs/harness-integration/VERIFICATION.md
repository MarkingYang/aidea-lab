# 实测记录

核对日期：2026-09-05。环境：macOS、Python 3.11.9、mcp 1.29.1；完整依赖见 requirements.txt。协商协议：2025-11-25。

## 已执行

命令：`python -m unittest discover -p 'test_*.py' -v`。

结果：16 项通过，包含 8 项模型响应边界和 8 项集成测试。测试时长不是性能基准，未进行吞吐或容量测量。

| 故障 | 中断时工单数 | 恢复后工单数 | 恢复后派发次数 | 提案保存次数 |
| --- | --- | --- | --- | --- |
| 服务端提交后退出 | 1 | 1 | 1 | 1 |
| 客户端保存意图后退出 | 0 | 1 | 2 | 1 |
| 客户端收到结果后退出 | 1 | 1 | 1 | 1 |

这些值由测试直接断言；每次测试使用独立临时目录。服务端 os._exit(73)、客户端 os._exit(74/75) 为真实进程退出，非抛出普通应用异常。

stdio 正常运行、终态重读、默认不写入、错误既有资源拒绝通过、任务契约变更拒绝恢复均已验证。本机 Streamable HTTP 正常创建和查询也已通过；测试服务器运行后会被清理。

## 未执行

没有调用外部模型 API，没有真实模型费用或质量数据。没有 HTTP 会话失效/SSE 重放、认证、沙箱、撤权、多 Worker、取消传播、迁移和断电实验。在线适配器不能标记为已完成生产验证。

## 复现范围

下载包只包含本文档、README、Python 源码、测试和固定依赖版本；不包含虚拟环境、密钥、数据库或本地运行日志。安装依赖需要网络；离线固定响应回归不需要模型账号。
