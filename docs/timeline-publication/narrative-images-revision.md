# 纪录片式叙事、阶段配图与 Obsidian 兼容修订

2026-09-16，用户在审阅叙事润色、六阶段配图和 Obsidian 图片路径修复后明确要求“发布”。本批仅发布时间线文章及其必要实现与记录，不包含杨植麟访谈或其他本地草稿。

## 文章与图片

保留 46 个日期节点、此前全部来源 URL、十二个横轴里程碑和 ReAct Mermaid 图。原先五项解释融入连续叙事，阶段小例子随事件展开；假设场景、原始研究与厂商演示仍有明确区别。标题采用用户修订的“回望 LLM 诞生的九年：从 LLM 到 Agent”，开篇将九年锚定到 2017 年 Transformer；URL 与原发布日期不变。

每个历史阶段增加一张现有来源图片，原始字节不变，未生成图片。来源、署名、版权边界及 SHA-256 见 `stage-images.json`。2022 年截图保留 CC BY-SA 4.0 署名与许可证；未将厂商图片声明为开放许可。本文仍是历史资料分析，未运行新的模型、Agent 或厂商基准实验。

## 阅读兼容

文稿的图片路径相对于各自 Markdown 文件：docs 使用 `../public/images/`，正式稿使用 `../../../public/images/`。`remark-public-images` 在构建时转换为网站 `/images/` 地址，仅处理 public 目录内的相对图片引用。图片设置等比尺寸；透明 Transformer 图的白底在放大查看时保留。两份正文在标准化图片路径后完全一致。

## 发布版本验证

- 从 origin/main 的 `4ca5d1a` 创建独立发布工作区，只复制本批文件。
- 图片路径转换的四项测试通过，覆盖两种稿件路径、HTML 属性保留、外部与越界路径、代码及普通链接。
- `npm run build` 通过，文章与图谱校验通过：75 篇文章、24 个分组、8 个方向、142 个图节点、601 条关系。
- 46 个日期标题与此前版本一致，既有来源 URL 全部保留，十二个横轴锚点有效，ReAct 源码保持一致。
- 六个图片文件 SHA-256 与来源清单一致；构建后的六个图片 URL 均为网站路径且对应静态文件存在；开发预览页未进入生产构建。
- 本次修订前已在 Obsidian 实际阅读界面确认本地图片加载，并核对开发文章页与预览页六张图片均返回成功。视觉检查范围不代表对全部历史事实的重新研究。
