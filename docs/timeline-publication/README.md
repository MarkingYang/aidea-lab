# LLM 到 Agent 时间线发布记录

2026-09-16，用户明确要求发布当前时间线文章与通用横向时间线组件。

- 正式页面：`/writing/llm-to-agent-evolution/`，归入现有 Harness 核心知识目录。
- 保留 38 个日期节点、每个节点五项说明与 38 个原始来源。文档源稿与正式正文一致，仅正式文章增加发布元数据、移除重复 H1；为现有内容检查补充 ReAct 图后的说明。
- 横向时间线使用静态 HTML/CSS 与轻量按钮增强；ReAct 循环保留 Mermaid。
- 独立发布版本：75 篇文章、24 个分组、8 个方向、142 个图节点、601 条关系。未纳入本任务之外的访谈草稿与历史存档。
- `node --test scripts/timeline.test.mjs`：4 项通过。
- `npm run build`：构建、文章检查、知识图谱检查通过。
- 正式构建页面：1440 / 390 像素、浅色 / 深色主题；检查时间线滚动、键盘首尾导航、单横轴对齐、来源与节点数、Mermaid 渲染及页面溢出，全部通过。见 `browser-results.json`。先前组件开发阶段还检查了多实例、禁用 JavaScript 和打印展开。
- `/preview/timeline/` 不在生产构建输出中。

本文属于文献与公开工程实践整理，未运行文章涉及的模型、Agent 或性能对比实验。历史来源日期与事实、解释、提案之间的边界保持不变。
