# 单项目文章六维重构结果

2026-09-06。覆盖 26 篇文章、16 个公开仓库，另更新源码研究方法页。保留项目和系列边界，同项目分篇共享安装与社区入口。知识图谱随站内链接自动更新。

- 每篇包含定位、架构、两个核心机制、快速上手、生态和源码路径。
- 技能资产与宿主执行分开；Claude Code 的公开插件不代替闭源核心。
- 架构图标明职责及请求／结果方向，动态流程补充时序图；保留已存在的解释性图片。
- 上手代码块均不超过 20 行，长 LangGraph 实验提供独立 Python 文件。依赖、认证与未运行范围明确标注。
- 仓库提交与 Issue 样本、许可证、固定提交和文件路径见同目录 JSON。维护者回复样本不外推 SLA。

实际验证：

- LangGraph 1.2.11 / Python 3.12：短暂停与恢复实验、完整通过与拒绝实验通过。
- ECC 固定提交：minimal/claude 计划返回 6 个模块、无警告；安装 Apply 测试 40 通过，Memory CLI 测试 15 通过。
- 其余真实模型调用、服务部署和上游测试仅静态核对，不声称实跑。
- Astro 构建与全量内容检查通过；知识图谱 148 节点、649 关系；16 项目录测试通过。
- 图片浏览器检查 36 项通过：已接入图片的桌面／手机显示、图注、放大、边缘滚动及无脚本回退。
- 浏览器全站检查 150 项通过，包括 88 篇文章导航、桌面／移动布局、无 JavaScript 导航与存储异常回退。

用户已要求暂停生成图片，聚焦内容收尾。此前已审核并接入正文的 8 张新增图片随本次提交保留；其他未接入资源不发布。后续计划及本地生成状态见 illustration-completion.json。
