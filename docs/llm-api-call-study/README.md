# 一次模型调用的完整路径

2026-09-17。用户要求结合所附信息图解释模型调用过程，随后要求总—分—总结构、校正流程语义，最后要求“文章整体原则简洁凝练，重点突出。搞定后发布”。

## 当前版本

`src/content/writing/llm-api-call-lifecycle.md` 保持原 URL 和日期，阅读时间从 23 分钟缩短为 10 分钟。汉字数从 7385 减为约 2892（包含元数据与图中文字）；删除公式、容量算例、次要优化机制表和两张重复细节图。保留一张开篇总览图、六个重点章节和结尾综合判断，不为篇幅目标填充内容。

正文重点为：输入准备、Prefill/Decode、KV 与前缀缓存、流式返回、工具执行、安全与计量。18 个早期研究来源的响应摘要保存在 `sources.json`，其中部分支撑已删去的扩展内容；当前正文就近保留直接支持现有论述的链接。

## 流程语义核对

本次不是只检查 Mermaid 能否显示，还逐条修正执行关系：

- 应用组织资料与服务端准备模型输入分开，不再将模板、分词固定在 HTTP 请求之前。图给出常见逻辑顺序，文字说明路由、分词位置可变，调度持续参与执行。
- Prefill、首 token 与按需 Decode 循环在同一推理节点内表达；KV 用存储节点和标明读取/写入的虚线表示，不是第六个执行步骤。正文区分常规缓存和跨请求前缀复用。
- 推理到应用的返回路径明确区分“流式：边生成边返回”和“非流式：结束后返回”。文本增量只进入展示/继续接收路径，不被当成完成。
- 应用解析后区分正常完成、完整工具请求、文本增量、错误或截断。工具执行前检查参数与权限，结果或失败信息沿显式回边接回上下文。图限定应用侧工具；正文另述托管工具责任与外部 API 请求边界。
- 安全、计量和记录放在包围全图的范围中，不再作为返回后的最后步骤。图省略各阶段错误连线，正文明确说明。

## 证据边界

- 用户图片仅作选题线索；未复制原图或将其当作已核实技术来源。本文 Mermaid 为原创解释图。
- 内容针对自回归 Transformer 文本服务。官方接口行为和开源机制不能证明闭源提供商的内部部署。
- `sources.json` 的 URL、检索时间与 SHA-256 对应当日 HTTP 响应，非不可变软件版本。完整第三方页面只暂存在 `/tmp/aidea-llm-api-sources/`，不纳入仓库。压缩前原稿只作本地临时对照，不发布另一篇文章。
- 没有调用模型、启动推理引擎或真实 Agent，没有执行业务工具、性能或费用实测。报告案例是假设；运行检查只验证文章与站点。
- 初稿及第一次结构调整保留过三张图、两处公式和容量算例。当前版本取代这些本地稿；不再以此前图表数量和验证结果描述当前稿。

## 发布范围

用户本次已授权通过 origin/main 发布这篇文章、现有模型目录入口及本目录研究记录。隔离发布基线是 `fb8a95a`，包含另一任务已经发布的 API/Runtime 文章；本次不改该文章，不包含未发布采访、首页修改、历史归档和 WeChat 文件。

隔离构建通过：78 篇文章、24 分组、8 方向；图谱 147 节点、624 连线。浏览器检查结果见 `browser-results.json`。这是发布树的数量，不混入本地未发布采访。

## 最终页面验证

以隔离发布树的生产构建提供静态页面，验证 1440px / 390px、浅色 / 深色四种组合：一张 Mermaid 图正常，无横向溢出或页面脚本错误，放大与复位正常。总览图经视觉检查，使用图内紧凑间距和既有放大/全屏控件，未修改站点渲染器。截图保存在本地 `/tmp/aidea-llm-api-final-*.png`。初次测试人为阻断外部评论服务造成 `Failed to fetch`；最终测试恢复正常页面网络加载后通过。
