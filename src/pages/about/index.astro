---
import BaseLayout from '../../layouts/BaseLayout.astro';
---

<BaseLayout title="关于｜大飞随笔" description="关于这座 AI 思考花园，以及它的记录方式。">
  <div class="page-shell about">
    <header class="page-intro">
      <div><p class="eyebrow">ABOUT THIS GARDEN</p><h1>为什么叫<br />“大飞随笔”？</h1></div>
      <p class="intro-copy">因为真正重要的理解，很少在第一次写下时就已经完成。这里更在意一个判断如何形成，又如何被新的事实修正。</p>
    </header>
    <div class="about-grid">
      <aside><span>RECORDING PRINCIPLES</span><p>记录变化，而不只记录结论。</p></aside>
      <article>
        <h2>这是什么</h2>
        <p>这是一个关于 AI、产品与人的个人研究花园。它收集短笔记、长篇思考和每周信号，也保存那些后来被证明并不准确的判断。</p>
        <h2>如何阅读</h2>
        <p>你可以按照时间阅读，也可以从一个主题出发。文章旁边的状态标记表达它目前的位置：“种子”只是直觉，“生长中”仍在修订，“常青”是暂时稳定的理解。</p>
        <h2>如何开始写</h2>
        <p>在 <code>src/content/writing</code> 中新建 Markdown 文件，填写标题、日期、类型、状态与主题，就会自动出现在对应页面中。</p>
      </article>
    </div>
  </div>
</BaseLayout>

<style>
  .about-grid { display: grid; grid-template-columns: .72fr 1.28fr; gap: clamp(40px, 7vw, 88px); margin-top: 56px; }
  aside { align-self: start; min-height: 220px; padding: 24px; display: flex; flex-direction: column; border-radius: var(--radius-lg); background: var(--surface); }
  aside span { color: var(--text-soft); font: 10px var(--mono); letter-spacing: .08em; }
  aside p { margin: auto 0 0; font-family: var(--display); font-size: 26px; line-height: 1.45; }
  article { max-width: 700px; }
  article h2 { margin: 0 0 12px; font-family: var(--display); font-size: 26px; font-weight: 400; }
  article h2:not(:first-child) { margin-top: 40px; }
  article p { margin: 0; color: var(--text-soft); font-size: 15px; line-height: 1.9; }
  code { padding: 3px 7px; border-radius: 5px; background: var(--surface); font: 12px var(--mono); }
  @media (max-width: 720px) { .about-grid { grid-template-columns: 1fr; } }
</style>
